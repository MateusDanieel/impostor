export const Round = ({
    players,
    turnIndex,
    secretWord,
    impostorIds,
    setPhase
}) => {
    const i = Math.floor(Math.random() * players.length);

    return (
        <>
            <h2>
                Quem começa é: <br />

            </h2>
        </>
    );
}